# Використовуйте офіційний образ Node.js як базовий
FROM node:18

# Встановіть робочий каталог всередині контейнера
WORKDIR /usr/src/app

# Скопіюйте файли package.json і package-lock.json
COPY package*.json ./

# Встановіть залежності
RUN npm install

# Скопіюйте решту коду програми
COPY . .

# Відкрити порт 3000 (або будь-який інший порт, який використовує ваша програма)
EXPOSE 3000

# Команда для запуску програми
CMD ["npm", "start"]
